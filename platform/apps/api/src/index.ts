/**
 * api — backend entrypoint (placeholder).
 *
 * The backend "brain": owns the DB + contract lifecycle, generates shareable
 * links, orchestrates the AI service, builds/submits Solana escrow txns, and
 * runs the verdict aggregator.
 *
 * Nothing is wired up yet. See README.md for the planned structure and TODOs.
 * Framework (Fastify vs NestJS vs Express) and DB layer are intentionally
 * undecided for now.
 */

console.log("api: placeholder entrypoint — not implemented yet");
