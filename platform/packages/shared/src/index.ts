/**
 * @repo/shared — shared TS domain + API types.
 *
 * Single source of truth for the contract lifecycle, verdicts, and AI DTOs,
 * imported by `web`, `admin`, and `api` so they can't drift.
 *
 * Placeholder — types to be added later (Contract, ContractStatus, Verdict, etc.).
 */

export {};
