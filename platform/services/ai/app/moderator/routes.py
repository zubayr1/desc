"""Moderator routes (placeholder).

    POST /verify  — evaluate one deliverable against the acceptance criteria,
                    returning a single moderator's verdict (pass/fail +
                    confidence + reasoning). `api` calls this N times across
                    different variants and aggregates the results.
"""
