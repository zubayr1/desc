"""Moderator variant configs (placeholder).

The platform's default federated pool: each variant is a distinct
prompt/model combination so the N moderators evaluate independently.
(V2 opens this pool to pluggable third-party operators.)
"""
